import styled from "styled-components";

export const StyledForm = styled.form`
  display: flex;
  flex-direction: column;
  gap: 1.5rem;

  width: 100%;

  .formFieldsContainer {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
`;
